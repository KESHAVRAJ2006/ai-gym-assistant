# Base URL of the FastAPI backend.
# Leave BLANK for local development - vite.config.js proxies /api and /ws
# to http://127.0.0.1:8000 so there is no CORS to configure.
#
# On Render, set this to your backend service URL, e.g.
#   VITE_API_BASE=https://aigym-api.onrender.com
VITE_API_BASE=
