# ---- Web framework ----
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
python-multipart>=0.0.9

# ---- Settings / validation ----
pydantic>=2.8,<3.0
pydantic-settings>=2.4,<3.0
email-validator>=2.2,<3.0

# ---- Relational DB ----
SQLAlchemy>=2.0,<3.0
alembic>=1.13,<2.0
psycopg[binary]>=3.2,<4.0

# ---- Auth ----
PyJWT>=2.9,<3.0
bcrypt>=4.1,<6.0

# ---- Document DB (optional at runtime) ----
pymongo>=4.8,<5.0

# ---- IoT ----
paho-mqtt>=2.1,<3.0

# ---- Science / ML ----
numpy>=1.26,<3.0
scikit-learn>=1.5,<2.0
xgboost>=2.1,<4.0
joblib>=1.4,<2.0

# ---- LLM coach (optional at runtime) ----
anthropic>=0.40,<2.0

# Deliberately NOT here: pandas (unused at runtime), matplotlib and
# pytest (see requirements-dev.txt). Render's free tier gives 512 MB of
# RAM and every import competes for it.
